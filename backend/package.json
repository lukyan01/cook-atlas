{
  "name": "cookatlas",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "NODE_ENV=test jest --runInBand --silent",
    "test:verbose": "NODE_ENV=test DEBUG=true jest --runInBand",
    "test:watch": "NODE_ENV=test jest --watch --runInBand --silent",
    "test:coverage": "NODE_ENV=test jest --coverage --runInBand --silent",
    "test:debug": "NODE_ENV=test DEBUG=true jest --runInBand --detectOpenHandles"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "pg": "^8.15.6"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.0",
    "supertest": "^7.1.0"
  }
}
